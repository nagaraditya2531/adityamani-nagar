/* The shared vocabulary. One file per topic.

   Categories have no page of their own — the listing at /topics/<name>/ is
   generated from them instead. The FILENAME is the address, so name files
   carefully and don't rename once you've linked to one. */
module.exports = { visibility: "public", permalink: false };
